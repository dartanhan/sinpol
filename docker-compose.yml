services:
  app:
    build:
      context: .
      dockerfile: docker/php/Dockerfile
    container_name: sinpol-app
    restart: unless-stopped
    working_dir: /var/www
    volumes:
      - .:/var/www
    networks:
      - sinpol-network
    depends_on:
      #  - mysql
      - redis

  nginx:
    image: nginx:stable-alpine
    container_name: sinpol-nginx
    restart: unless-stopped
    ports:
      - "9004:80"
    volumes:
      - .:/var/www
      - ./docker/nginx/conf.d/:/etc/nginx/conf.d/
    networks:
      - sinpol-network

  #mysql: usando na porta 3309 mysql padrão
  #  image: mysql:8.0
  #  container_name: sinpol-mysql
  # restart: unless-stopped
  # environment:
  #   MYSQL_DATABASE: sinpol
  ##   MYSQL_ROOT_PASSWORD: root
  # ports:
  #   - "3306:3306"
  # volumes:
  #   - mysql-data:/var/lib/mysql
  # networks:
  #   - sinpol-network

  redis:
    image: redis:alpine
    container_name: sinpol-redis
    restart: unless-stopped
    networks:
      - sinpol-network

networks:
  sinpol-network:
    driver: bridge

volumes:
  mysql-data:
